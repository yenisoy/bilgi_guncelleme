const User = require('./User');
const Person = require('./Person');
const AddressChange = require('./AddressChange');
const AddressCache = require('./AddressCache');

module.exports = {
    User,
    Person,
    AddressChange,
    AddressCache
};
